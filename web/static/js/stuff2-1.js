/* const cbtn = document.querySelectorAll(".subcontainer");

cbtn.forEach((btn)) => {
    btn.addEventListener("click", (e => {
	cbtn.forEach(f => f.classList.remove('active'));
	e.target.classList.toggle("active");
    });
}); 
*/
  